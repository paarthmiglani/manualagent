# Interface Module - src/interface/__init__.py

# This file makes 'interface' a Python sub-package.
# It can be used to launch the main application or expose UI components.

# Example: If using Streamlit, you might have a main function in streamlit_app.py
# from .streamlit_app import main as run_streamlit_app

VERSION = "0.1.0"

print(f"Interface module (version {VERSION}) initialized.")

# __all__ = ['run_streamlit_app']
