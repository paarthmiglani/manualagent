# Pipeline Module - src/pipeline/__init__.py

# This file makes 'pipeline' a Python sub-package.
# It can be used to expose the main functionalities of the full processing pipeline.

# Example imports (actual classes/functions will be defined in other files):
# from .artifact_processor import ArtifactProcessor
# from .multimodal_query import MultimodalQueryHandler

VERSION = "0.1.0"

print(f"Pipeline module (version {VERSION}) initialized.")

# __all__ = ['ArtifactProcessor', 'MultimodalQueryHandler']
