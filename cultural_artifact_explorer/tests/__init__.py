# This file makes 'tests' a Python package.
# It's often kept empty or might contain shared test fixtures or utilities
# if using pytest's conftest.py features at a higher level.

print("Test package initialized.")
